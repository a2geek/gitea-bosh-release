{
<% if p("database.type") == "postgres" -%>
  "database.gitea.internal": [
    "*.postgres.*.gitea.bosh"
  ]
<% end -%>
<% if p("database.type") == "mysql" -%>
  "database.gitea.internal": [
    "*.mysql.*.gitea.bosh"
  ]
<% end -%>
}
