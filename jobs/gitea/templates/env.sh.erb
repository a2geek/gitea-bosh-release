# Handy environment setup.

export PATH=/var/vcap/packages/git/bin:$PATH
export HOME=/var/vcap/store/gitea

export GITEA="/var/vcap/packages/gitea/bin/gitea --config /var/vcap/jobs/gitea/config/app.ini"
if [ "$(whoami)" != "vcap" ]
then
    export GITEA="chpst -u vcap:vcap ${GITEA}"
fi
